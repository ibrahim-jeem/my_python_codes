# this code will show the total of a factorial of a value
#exp: 2! = 1x2 = 2 or 4! = 1x2x3x4 = 24

#defining the function
def factorial(n):
    #initial result 
    result = 1
    #foor loop for going through the multiplication of the factorial numbers
    for i in range(1, n):
        #(i+1) to increase for next digit to multiply
        result = result * (i+1)
    return result

print("Give the number: ")
print("Result is = ", factorial(int(input())))
